---
title: "Seasonal/Annual Surface Temperatures (GrIS)"
author: "Shayne O'Brien"
date: "January 28, 2019"
output:
  html_document:
    toc: true
    toc_float: true
    theme: united
    toc_depth: 3
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```
#Objective
The object of this item is to create Tables and Time Series for each year and each season for the PROMICE AWSs between 2009 and 2018.

The following packages are needed to reproduce these results

```{r,message=FALSE, error=FALSE}
library(tidyr)
library(dplyr)
library(ggplot2)
library(DT)
library(leaflet)
```
#Interactive Map
Click each of the markers to locate each station examined in the study. Zoom to view location on the Ice Sheet.

 *   On independent glacier
 **  On land
 *** Discontinued
```{r, echo=FALSE}
locations <- read.csv(header = T, "PROMICE Stations.csv")

leaflet(locations) %>% 
  addTiles() %>% 
  addMarkers(~ Long, ~ Lat, popup = ~Station_Name)

```

#Kangerlussaq Transect

```{r, echo = FALSE}
kan_u_df <- read.table(header = T, "Kan_U_day.txt") %>% 
  subset(AirTemperature.C. != -999)
kan_m_df <- read.table(header = T, "Kan_M_day.txt") %>% 
  subset(AirTemperature.C. != -999)
kan_l_df <- read.table(header = T, "Kan_L_day2.txt") %>% 
  subset(AirTemperature.C. != -999)
```

```{r, echo = FALSE}
##Create Target Table
#Create a table for only Year, Month, Day, Air temperature.
#Then, add a new binary column, 1 for >0C, 0 for <0C
KAN_U <- kan_u_df %>% 
  select(Year, MonthOfYear, AirTemperature.C.) 
  KAN_U$Melt <- as.numeric(KAN_U$AirTemperature.C. > 0)
  
KAN_M <- kan_m_df %>% 
  select(Year, MonthOfYear, DayOfMonth, AirTemperature.C.)
  KAN_M$Melt <- as.numeric(KAN_M$AirTemperature.C. > 0)
  
KAN_L <- kan_l_df %>% 
  select(Year, MonthOfYear, DayOfMonth, AirTemperature.C.)  
  KAN_L$Melt <- as.numeric(KAN_L$AirTemperature.C. > 0)
  
  
```

##Monthly and Annual Tables

###KAN U  
```{r, echo=FALSE}
kan_u_season <- KAN_U %>%
  rename(Month = MonthOfYear) %>% 
  mutate(Year = as.numeric(Year)) %>%
  group_by(Year, Month) %>%
  summarise(MeltDays = sum(Melt)) %>% 
  spread(Month, MeltDays)
kan_u_annual <- KAN_U %>%
  select(Year, Melt) %>% 
  mutate(Year = as.numeric(Year)) %>%
  group_by(Year) %>%
  summarise(MeltDays = sum(Melt))
  
datatable(
  kan_u_season, class = 'cell-border stripe', 
  rownames = FALSE, 
  colnames = c('Jan', 'Feb','Mar', 'Apr', 'May', 'Jun', 'July', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'),
  caption = htmltools::tags$caption(
  style = 'caption-side: bottom; text-align: center,',
  'Table 1: ', htmltools::em('This table displays the number of days per month with a temperature of > 0deg Celsius, 2009-2018')
  ))
datatable(
  kan_u_annual, class = 'cell-border stripe', 
  rownames = FALSE,
  caption = htmltools::tags$caption(
  style = 'caption-side: bottom; text-align: center,',
  'Table 1: ', htmltools::em('This table displays the number of days annually with a temperature of > 0deg Celsius, 2009-2018')
  ))
##c(Melt,Year(2009)))summarise(KAN_U, sum = sum(Melt))
#summarise(KAN_U, "2009" = sum(
```

### KAN M  

```{r, echo = FALSE}
kan_m_season <- KAN_M %>%
  rename(Month = MonthOfYear) %>% 
  mutate(Year = as.numeric(Year)) %>%
  group_by(Year, Month) %>%
  summarise(MeltDays = sum(Melt)) %>% 
  spread(Month, MeltDays)
kan_m_annual <- KAN_M %>%
  select(Year, Melt) %>% 
  mutate(Year = as.numeric(Year)) %>%
  group_by(Year) %>%
  summarise(MeltDays = sum(Melt))
  
datatable(
  kan_m_season, class = 'cell-border stripe', 
  rownames = FALSE, 
  colnames = c('Jan', 'Feb','Mar', 'Apr', 'May', 'Jun', 'July', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'),
  caption = htmltools::tags$caption(
  style = 'caption-side: bottom; text-align: center,',
  'Table 1: ', htmltools::em('This table displays the number of days per month with a temperature of > 0deg Celsius, 2009-2018')
  ))
datatable(
  kan_m_annual, class = 'cell-border stripe', 
  rownames = FALSE,
  caption = htmltools::tags$caption(
  style = 'caption-side: bottom; text-align: center,',
  'Table 1: ', htmltools::em('This table displays the number of days annually with a temperature of > 0deg Celsius, 2009-2018')
  ))
```

### KAN L  

```{r, echo=FALSE}
kan_l_season <- KAN_L %>%
  rename(Month = MonthOfYear) %>% 
  mutate(Year = as.numeric(Year)) %>%
  group_by(Year, Month) %>%
  summarise(MeltDays = sum(Melt)) %>% 
  spread(Month, MeltDays)
kan_l_annual <- KAN_L %>%
  select(Year, Melt) %>% 
  mutate(Year = as.numeric(Year)) %>%
  group_by(Year) %>%
  summarise(MeltDays = sum(Melt))
  
datatable(
  kan_l_season, class = 'cell-border stripe', 
  rownames = FALSE, 
  colnames = c('Jan', 'Feb','Mar', 'Apr', 'May', 'Jun', 'July', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'),
  caption = htmltools::tags$caption(
  style = 'caption-side: bottom; text-align: center,',
  'Table 1: ', htmltools::em('This table displays the number of days per month with a temperature of > 0deg Celsius, 2009-2018')
  ))
datatable(
  kan_l_annual, class = 'cell-border stripe', 
  rownames = FALSE,
  caption = htmltools::tags$caption(
  style = 'caption-side: bottom; text-align: center,',
  'Table 1: ', htmltools::em('This table displays the number of days annually with a temperature of > 0deg Celsius, 2009-2018')
  ))
```

## Surface Temperature Timeseries

### KAN U   

```{r fig.height=10, fig.width=12, echo = FALSE}
kan_u_plot <- kan_u_df
kan_u_plot$Date <- as.Date(with(kan_u_plot, paste(Year, MonthOfYear, DayOfMonth,sep="-")), "%d-%m-%Y")
   
   
ggplot(kan_u_plot, aes(DayOfYear, AirTemperature.C.))+
  geom_line()+
  facet_wrap(~Year,scales = "fixed", ncol = 2)+
  xlab("Day of Year")+
  ylab("Degrees C")
  
  
  
  
```

### KAN M  

```{r fig.height=10, fig.width=12, echo = FALSE}
kan_m_plot <- kan_m_df
#kan_m_plot$Date <- as.Date(with(kan_m_plot, paste(Year, MonthOfYear, DayOfMonth,sep="-")), "%d-%m-%Y")
   
   
ggplot(kan_m_plot, aes(DayOfYear, AirTemperature.C.))+
  geom_line()+
  facet_wrap(~Year,scales = "fixed", ncol = 2)+
  xlab("Day of Year")+
  ylab("Degrees C")
  
  
```

### KAN L  

```{r fig.height=10, fig.width=12, echo = FALSE}
kan_l_plot <- kan_l_df
#kan_l_plot$Date <- as.Date(with(kan_u_plot, paste(Year, MonthOfYear, DayOfMonth,sep="-")), "%d-%m-%Y")
   
   
ggplot(kan_l_plot, aes(DayOfYear, AirTemperature.C.))+
  geom_line()+
  facet_wrap(~Year,scales = "fixed", ncol = 2)+
  xlab("Day of Year")+
  ylab("Degrees C")
  
```

#Nuuk Transect

```{r, echo = FALSE}
nuk_k_df <- read.table(header = T, "NUK_K_day.txt") %>%  
  subset(AirTemperature.C. != -999)
nuk_l_df <- read.table(header = T, "NUK_L_day.txt") %>% 
  subset(AirTemperature.C. != -999)
nuk_u_df <- read.table(header = T, "NUK_U_day.txt") %>% 
  subset(AirTemperature.C. != -999)
```

```{r, echo = FALSE}
##Create Target Table
#Create a table for only Year, Month, Day, Air temperature.
#Then, add a new binary column, 1 for >0C, 0 for <0C
NUK_K <- nuk_k_df %>% 
  select(Year, MonthOfYear, AirTemperature.C.) 
  NUK_K$Melt <- as.numeric(NUK_K$AirTemperature.C. > 0)
  
NUK_L <- nuk_l_df %>% 
  select(Year, MonthOfYear, DayOfMonth, AirTemperature.C.)
  NUK_L$Melt <- as.numeric(NUK_L$AirTemperature.C. > 0)
NUK_U <- nuk_u_df %>% 
  select(Year, MonthOfYear, DayOfMonth, AirTemperature.C.)  
  NUK_U$Melt <- as.numeric(NUK_U$AirTemperature.C. > 0)
  
  
```
##Monthly and Annual Tables

###NUK K  
```{r, echo=FALSE}
nuk_k_season <- NUK_K %>%
  rename(Month = MonthOfYear) %>% 
  mutate(Year = as.numeric(Year)) %>%
  group_by(Year, Month) %>%
  summarise(MeltDays = sum(Melt)) %>% 
  spread(Month, MeltDays)
nuk_k_annual <- NUK_K %>%
  select(Year, Melt) %>% 
  mutate(Year = as.numeric(Year)) %>%
  group_by(Year) %>%
  summarise(MeltDays = sum(Melt))
  
datatable(
  nuk_k_season, class = 'cell-border stripe', 
  rownames = FALSE, 
  colnames = c('Jan', 'Feb','Mar', 'Apr', 'May', 'Jun', 'July', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'),
  caption = htmltools::tags$caption(
  style = 'caption-side: bottom; text-align: center,',
  'Table 1: ', htmltools::em('This table displays the number of days per month with a temperature of > 0deg Celsius, 2009-2018')
  ))
datatable(
  nuk_k_annual, class = 'cell-border stripe', 
  rownames = FALSE,
  caption = htmltools::tags$caption(
  style = 'caption-side: bottom; text-align: center,',
  'Table 1: ', htmltools::em('This table displays the number of days annually with a temperature of > 0deg Celsius, 2009-2018')
  ))
##c(Melt,Year(2009)))summarise(KAN_U, sum = sum(Melt))
#summarise(KAN_U, "2009" = sum(
```

### NUK L  

```{r, echo = FALSE}
nuk_l_season <- NUK_L %>%
  rename(Month = MonthOfYear) %>% 
  mutate(Year = as.numeric(Year)) %>%
  group_by(Year, Month) %>%
  summarise(MeltDays = sum(Melt)) %>% 
  spread(Month, MeltDays)
nuk_l_annual <- NUK_L %>%
  select(Year, Melt) %>% 
  mutate(Year = as.numeric(Year)) %>%
  group_by(Year) %>%
  summarise(MeltDays = sum(Melt))
  
datatable(
  nuk_l_season, class = 'cell-border stripe', 
  rownames = FALSE, 
  colnames = c('Jan', 'Feb','Mar', 'Apr', 'May', 'Jun', 'July', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'),
  caption = htmltools::tags$caption(
  style = 'caption-side: bottom; text-align: center,',
  'Table 1: ', htmltools::em('This table displays the number of days per month with a temperature of > 0deg Celsius, 2009-2018')
  ))
datatable(
  nuk_l_annual, class = 'cell-border stripe', 
  rownames = FALSE,
  caption = htmltools::tags$caption(
  style = 'caption-side: bottom; text-align: center,',
  'Table 1: ', htmltools::em('This table displays the number of days annually with a temperature of > 0deg Celsius, 2009-2018')
  ))
```

### NUK U  

```{r, echo=FALSE}
nuk_u_season <- NUK_U %>%
  rename(Month = MonthOfYear) %>% 
  mutate(Year = as.numeric(Year)) %>%
  group_by(Year, Month) %>%
  summarise(MeltDays = sum(Melt)) %>% 
  spread(Month, MeltDays)
nuk_u_annual <- NUK_U %>%
  select(Year, Melt) %>% 
  mutate(Year = as.numeric(Year)) %>%
  group_by(Year) %>%
  summarise(MeltDays = sum(Melt))
  
datatable(
  nuk_u_season, class = 'cell-border stripe', 
  rownames = FALSE, 
  colnames = c('Jan', 'Feb','Mar', 'Apr', 'May', 'Jun', 'July', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'),
  caption = htmltools::tags$caption(
  style = 'caption-side: bottom; text-align: center,',
  'Table 1: ', htmltools::em('This table displays the number of days per month with a temperature of > 0deg Celsius, 2009-2018')
  ))
datatable(
  nuk_u_annual, class = 'cell-border stripe', 
  rownames = FALSE,
  caption = htmltools::tags$caption(
  style = 'caption-side: bottom; text-align: center,',
  'Table 1: ', htmltools::em('This table displays the number of days annually with a temperature of > 0deg Celsius, 2009-2018')
  ))
```

## Surface Temperature Timeseries

### NUK K   

```{r fig.height=10, fig.width=12, echo = FALSE}
nuk_k_plot <- nuk_k_df
#nuk_k_plot$Date <- as.Date(with(nuk_k_plot, paste(Year, MonthOfYear, DayOfMonth,sep="-")), "%d-%m-%Y")
   
   
ggplot(nuk_k_plot, aes(DayOfYear, AirTemperature.C.))+
  geom_line()+
  facet_wrap(~Year,scales = "fixed", ncol = 2, nrow = 10)+
  xlab("Day of Year")+
  ylab("Degrees C")
  
  
  
  
```

### NUK L  

```{r fig.height=10, fig.width=12, echo = FALSE}
nuk_l_plot <- nuk_l_df
#kan_m_plot$Date <- as.Date(with(kan_m_plot, paste(Year, MonthOfYear, DayOfMonth,sep="-")), "%d-%m-%Y")
   
   
ggplot(nuk_l_plot, aes(DayOfYear, AirTemperature.C.))+
  geom_line()+
  facet_wrap(~Year,scales = "fixed", ncol = 2)+
  xlab("Day of Year")+
  ylab("Degrees C")
  
  
```

### NUK U  

```{r fig.height=10, fig.width=12, echo = FALSE}
nuk_u_plot <- nuk_u_df
#kan_l_plot$Date <- as.Date(with(kan_u_plot, paste(Year, MonthOfYear, DayOfMonth,sep="-")), "%d-%m-%Y")
   
   
ggplot(nuk_u_plot, aes(DayOfYear, AirTemperature.C.))+
  geom_line()+
  facet_wrap(~Year,scales = "fixed", ncol = 2)+
  xlab("Day of Year")+
  ylab("Degrees C")
  
```

#Qassimiut Transect

```{r, echo = FALSE}
qas_a_df <- read.table(header = T, "QAS_A_day.txt") %>%  
  subset(AirTemperature.C. != -999)
qas_l_df <- read.table(header = T, "QAS_L_day.txt") %>% 
  subset(AirTemperature.C. != -999)
qas_m_df <- read.table(header = T, "QAS_M_day.txt") %>% 
  subset(AirTemperature.C. != -999)
qas_u_df <- read.table(header = T, "QAS_U_day.txt") %>% 
  subset(AirTemperature.C. != -999)
```

```{r, echo = FALSE}
##Create Target Table
#Create a table for only Year, Month, Day, Air temperature.
#Then, add a new binary column, 1 for >0C, 0 for <0C
QAS_A <- qas_a_df %>% 
  select(Year, MonthOfYear, DayOfMonth, AirTemperature.C.)
  QAS_A$Melt <- as.numeric(QAS_A$AirTemperature.C. > 0)
  
QAS_L <- qas_l_df %>% 
  select(Year, MonthOfYear, DayOfMonth, AirTemperature.C.)
  QAS_L$Melt <- as.numeric(QAS_L$AirTemperature.C. > 0)
  
QAS_M <- qas_m_df %>% 
  select(Year, MonthOfYear, DayOfMonth, AirTemperature.C.)  
  QAS_M$Melt <- as.numeric(QAS_M$AirTemperature.C. > 0)
  
QAS_U <- qas_u_df %>% 
  select(Year, MonthOfYear, DayOfMonth, AirTemperature.C.)  
  QAS_U$Melt <- as.numeric(QAS_U$AirTemperature.C. > 0)
  
  
```

##Monthly and Annual Tables

###QAS A 

```{r, echo=FALSE}
qas_a_season <- QAS_A %>%
  rename(Month = MonthOfYear) %>% 
  mutate(Year = as.numeric(Year)) %>%
  group_by(Year, Month) %>%
  summarise(MeltDays = sum(Melt)) %>% 
  spread(Month, MeltDays)
qas_a_annual <- QAS_A %>%
  select(Year, Melt) %>% 
  mutate(Year = as.numeric(Year)) %>%
  group_by(Year) %>%
  summarise(MeltDays = sum(Melt))
  
datatable(
  qas_a_season, class = 'cell-border stripe', 
  rownames = FALSE, 
  colnames = c('Jan', 'Feb','Mar', 'Apr', 'May', 'Jun', 'July', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'),
  caption = htmltools::tags$caption(
  style = 'caption-side: bottom; text-align: center,',
  'Table 1: ', htmltools::em('This table displays the number of days per month with a temperature of > 0deg Celsius, 2009-2018')
  ))
datatable(
  qas_a_annual, class = 'cell-border stripe', 
  rownames = FALSE,
  caption = htmltools::tags$caption(
  style = 'caption-side: bottom; text-align: center,',
  'Table 1: ', htmltools::em('This table displays the number of days annually with a temperature of > 0deg Celsius, 2009-2018')
  ))
##c(Melt,Year(2009)))summarise(KAN_U, sum = sum(Melt))
#summarise(KAN_U, "2009" = sum(
```

### QAS L  

```{r, echo = FALSE}
qas_l_season <- QAS_L %>%
  rename(Month = MonthOfYear) %>% 
  mutate(Year = as.numeric(Year)) %>%
  group_by(Year, Month) %>%
  summarise(MeltDays = sum(Melt)) %>% 
  spread(Month, MeltDays)
qas_l_annual <- QAS_L %>%
  select(Year, Melt) %>% 
  mutate(Year = as.numeric(Year)) %>%
  group_by(Year) %>%
  summarise(MeltDays = sum(Melt))
  
datatable(
  qas_l_season, class = 'cell-border stripe', 
  rownames = FALSE, 
  colnames = c('Jan', 'Feb','Mar', 'Apr', 'May', 'Jun', 'July', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'),
  caption = htmltools::tags$caption(
  style = 'caption-side: bottom; text-align: center,',
  'Table 1: ', htmltools::em('This table displays the number of days per month with a temperature of > 0deg Celsius, 2009-2018')
  ))
datatable(
  qas_l_annual, class = 'cell-border stripe', 
  rownames = FALSE,
  caption = htmltools::tags$caption(
  style = 'caption-side: bottom; text-align: center,',
  'Table 1: ', htmltools::em('This table displays the number of days annually with a temperature of > 0deg Celsius, 2009-2018')
  ))
```


###QAS M  

```{r, echo=FALSE}
qas_m_season <- QAS_M %>%
  rename(Month = MonthOfYear) %>% 
  mutate(Year = as.numeric(Year)) %>%
  group_by(Year, Month) %>%
  summarise(MeltDays = sum(Melt)) %>% 
  spread(Month, MeltDays)
qas_m_annual <- QAS_M %>%
  select(Year, Melt) %>% 
  mutate(Year = as.numeric(Year)) %>%
  group_by(Year) %>%
  summarise(MeltDays = sum(Melt))
  
datatable(
  qas_m_season, class = 'cell-border stripe', 
  rownames = FALSE, 
  colnames = c('Jan', 'Feb','Mar', 'Apr', 'May', 'Jun', 'July', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'),
  caption = htmltools::tags$caption(
  style = 'caption-side: bottom; text-align: center,',
  'Table 1: ', htmltools::em('This table displays the number of days per month with a temperature of > 0deg Celsius, 2009-2018')
  ))
datatable(
  qas_m_annual, class = 'cell-border stripe', 
  rownames = FALSE,
  caption = htmltools::tags$caption(
  style = 'caption-side: bottom; text-align: center,',
  'Table 1: ', htmltools::em('This table displays the number of days annually with a temperature of > 0deg Celsius, 2009-2018')
  ))
```

###QAS U

```{r, echo=FALSE}
qas_u_season <- QAS_U %>%
  rename(Month = MonthOfYear) %>% 
  mutate(Year = as.numeric(Year)) %>%
  group_by(Year, Month) %>%
  summarise(MeltDays = sum(Melt)) %>% 
  spread(Month, MeltDays)
qas_u_annual <- QAS_U %>%
  select(Year, Melt) %>% 
  mutate(Year = as.numeric(Year)) %>%
  group_by(Year) %>%
  summarise(MeltDays = sum(Melt))
  
datatable(
  qas_u_season, class = 'cell-border stripe', 
  rownames = FALSE, 
  colnames = c('Jan', 'Feb','Mar', 'Apr', 'May', 'Jun', 'July', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'),
  caption = htmltools::tags$caption(
  style = 'caption-side: bottom; text-align: center,',
  'Table 1: ', htmltools::em('This table displays the number of days per month with a temperature of > 0deg Celsius, 2009-2018')
  ))
datatable(
  qas_u_annual, class = 'cell-border stripe', 
  rownames = FALSE,
  caption = htmltools::tags$caption(
  style = 'caption-side: bottom; text-align: center,',
  'Table 1: ', htmltools::em('This table displays the number of days annually with a temperature of > 0deg Celsius, 2009-2018')
  ))
```

## Surface Temperature Timeseries

### QAS A 

```{r fig.height=10, fig.width=12, echo = FALSE}
nuk_k_plot <- nuk_k_df
#nuk_k_plot$Date <- as.Date(with(nuk_k_plot, paste(Year, MonthOfYear, DayOfMonth,sep="-")), "%d-%m-%Y")
   
   
ggplot(nuk_k_plot, aes(DayOfYear, AirTemperature.C.))+
  geom_line()+
  facet_wrap(~Year,scales = "fixed", ncol = 2, nrow = 10)+
  xlab("Day of Year")+
  ylab("Degrees C")
  
  
  
  
```

### QAS L  

```{r fig.height=10, fig.width=12, echo = FALSE}
qas_l_plot <- qas_l_df
#kan_m_plot$Date <- as.Date(with(kan_m_plot, paste(Year, MonthOfYear, DayOfMonth,sep="-")), "%d-%m-%Y")
   
   
ggplot(qas_l_plot, aes(DayOfYear, AirTemperature.C.))+
  geom_line()+
  facet_wrap(~Year,scales = "fixed", ncol = 2)+
  xlab("Day of Year")+
  ylab("Degrees C")
  
  
```

### QAS M  

```{r fig.height=10, fig.width=12, echo = FALSE}
qas_m_plot <- qas_m_df
#kan_l_plot$Date <- as.Date(with(kan_u_plot, paste(Year, MonthOfYear, DayOfMonth,sep="-")), "%d-%m-%Y")
   
   
ggplot(qas_m_plot, aes(DayOfYear, AirTemperature.C.))+
  geom_line()+
  facet_wrap(~Year,scales = "fixed", ncol = 2)+
  xlab("Day of Year")+
  ylab("Degrees C")
  
```

### QAS U   

```{r fig.height=10, fig.width=12, echo = FALSE}
qas_u_plot <- qas_l_df
#nuk_k_plot$Date <- as.Date(with(nuk_k_plot, paste(Year, MonthOfYear, DayOfMonth,sep="-")), "%d-%m-%Y")
   
   
ggplot(qas_l_plot, aes(DayOfYear, AirTemperature.C.))+
  geom_line()+
  facet_wrap(~Year,scales = "fixed", ncol = 2, nrow = 10)+
  xlab("Day of Year")+
  ylab("Degrees C")
  
```

#Tasiilaq Transect

```{r, echo = FALSE}
mit_df <- read.table(header = T, "MIT_day.txt") %>%  
  subset(AirTemperature.C. != -999)
tas_a_df <- read.table(header = T, "TAS_A_day.txt") %>% 
  subset(AirTemperature.C. != -999)
tas_l_df <- read.table(header = T, "TAS_L_day.txt") %>% 
  subset(AirTemperature.C. != -999)
```

```{r, echo = FALSE}
##Create Target Table
#Create a table for only Year, Month, Day, Air temperature.
#Then, add a new binary column, 1 for >0C, 0 for <0C
MIT <- mit_df %>% 
  select(Year, MonthOfYear, AirTemperature.C.) 
  MIT$Melt <- as.numeric(MIT$AirTemperature.C. > 0)
  
TAS_A <- tas_a_df %>% 
  select(Year, MonthOfYear, DayOfMonth, AirTemperature.C.)
  TAS_A$Melt <- as.numeric(TAS_A$AirTemperature.C. > 0)
  
TAS_L <- tas_l_df %>% 
  select(Year, MonthOfYear, DayOfMonth, AirTemperature.C.)  
  TAS_L$Melt <- as.numeric(TAS_L$AirTemperature.C. > 0)
  
  
```
##Monthly and Annual Tables

###MIT  
```{r, echo=FALSE}
mit_season <- MIT %>%
  rename(Month = MonthOfYear) %>% 
  mutate(Year = as.numeric(Year)) %>%
  group_by(Year, Month) %>%
  summarise(MeltDays = sum(Melt)) %>% 
  spread(Month, MeltDays)
mit_annual <- MIT %>%
  select(Year, Melt) %>% 
  mutate(Year = as.numeric(Year)) %>%
  group_by(Year) %>%
  summarise(MeltDays = sum(Melt))
  
datatable(
  mit_season, class = 'cell-border stripe', 
  rownames = FALSE, 
  colnames = c('Jan', 'Feb','Mar', 'Apr', 'May', 'Jun', 'July', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'),
  caption = htmltools::tags$caption(
  style = 'caption-side: bottom; text-align: center,',
  'Table 1: ', htmltools::em('This table displays the number of days per month with a temperature of > 0deg Celsius, 2009-2018')
  ))
datatable(
  mit_annual, class = 'cell-border stripe', 
  rownames = FALSE,
  caption = htmltools::tags$caption(
  style = 'caption-side: bottom; text-align: center,',
  'Table 1: ', htmltools::em('This table displays the number of days annually with a temperature of > 0deg Celsius, 2009-2018')
  ))
##c(Melt,Year(2009)))summarise(KAN_U, sum = sum(Melt))
#summarise(KAN_U, "2009" = sum(
```

### TAS A  

```{r, echo = FALSE}
tas_a_season <- TAS_A %>%
  rename(Month = MonthOfYear) %>% 
  mutate(Year = as.numeric(Year)) %>%
  group_by(Year, Month) %>%
  summarise(MeltDays = sum(Melt)) %>% 
  spread(Month, MeltDays)
tas_a_annual <- TAS_A %>%
  select(Year, Melt) %>% 
  mutate(Year = as.numeric(Year)) %>%
  group_by(Year) %>%
  summarise(MeltDays = sum(Melt))
  
datatable(
  tas_a_season, class = 'cell-border stripe', 
  rownames = FALSE, 
  colnames = c('Jan', 'Feb','Mar', 'Apr', 'May', 'Jun', 'July', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'),
  caption = htmltools::tags$caption(
  style = 'caption-side: bottom; text-align: center,',
  'Table 1: ', htmltools::em('This table displays the number of days per month with a temperature of > 0deg Celsius, 2009-2018')
  ))
datatable(
  tas_a_annual, class = 'cell-border stripe', 
  rownames = FALSE,
  caption = htmltools::tags$caption(
  style = 'caption-side: bottom; text-align: center,',
  'Table 1: ', htmltools::em('This table displays the number of days annually with a temperature of > 0deg Celsius, 2009-2018')
  ))
```

### TAS L  

```{r, echo=FALSE}
tas_l_season <- TAS_L %>%
  rename(Month = MonthOfYear) %>% 
  mutate(Year = as.numeric(Year)) %>%
  group_by(Year, Month) %>%
  summarise(MeltDays = sum(Melt)) %>% 
  spread(Month, MeltDays)
tas_l_annual <- TAS_L %>%
  select(Year, Melt) %>% 
  mutate(Year = as.numeric(Year)) %>%
  group_by(Year) %>%
  summarise(MeltDays = sum(Melt))
  
datatable(
  tas_l_season, class = 'cell-border stripe', 
  rownames = FALSE, 
  colnames = c('Jan', 'Feb','Mar', 'Apr', 'May', 'Jun', 'July', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'),
  caption = htmltools::tags$caption(
  style = 'caption-side: bottom; text-align: center,',
  'Table 1: ', htmltools::em('This table displays the number of days per month with a temperature of > 0deg Celsius, 2009-2018')
  ))
datatable(
  tas_l_annual, class = 'cell-border stripe', 
  rownames = FALSE,
  caption = htmltools::tags$caption(
  style = 'caption-side: bottom; text-align: center,',
  'Table 1: ', htmltools::em('This table displays the number of days annually with a temperature of > 0deg Celsius, 2009-2018')
  ))
```

## Surface Temperature Timeseries

### MIT   

```{r fig.height=10, fig.width=12, echo = FALSE}
mit_plot <- mit_df
#nuk_k_plot$Date <- as.Date(with(nuk_k_plot, paste(Year, MonthOfYear, DayOfMonth,sep="-")), "%d-%m-%Y")
   
   
ggplot(mit_plot, aes(DayOfYear, AirTemperature.C.))+
  geom_line()+
  facet_wrap(~Year,scales = "fixed", ncol = 2, nrow = 10)+
  xlab("Day of Year")+
  ylab("Degrees C")
  
  
  
  
```

### TAS A  

```{r fig.height=10, fig.width=12, echo = FALSE}
tas_a_plot <- tas_a_df
#kan_m_plot$Date <- as.Date(with(kan_m_plot, paste(Year, MonthOfYear, DayOfMonth,sep="-")), "%d-%m-%Y")
   
   
ggplot(tas_a_plot, aes(DayOfYear, AirTemperature.C.))+
  geom_line()+
  facet_wrap(~Year,scales = "fixed", ncol = 2)+
  xlab("Day of Year")+
  ylab("Degrees C")
  
  
```

### TAS L  

```{r fig.height=10, fig.width=12, echo = FALSE}
tas_l_plot <- tas_l_df
#kan_l_plot$Date <- as.Date(with(kan_u_plot, paste(Year, MonthOfYear, DayOfMonth,sep="-")), "%d-%m-%Y")
   
   
ggplot(tas_l_plot, aes(DayOfYear, AirTemperature.C.))+
  geom_line()+
  facet_wrap(~Year,scales = "fixed", ncol = 2)+
  xlab("Day of Year")+
  ylab("Degrees C")
  
```

#Upernavik Transect

```{r, echo = FALSE}
upe_l_df <- read.table(header = T, "UPE_L_day.txt") %>%  
  subset(AirTemperature.C. != -999)
upe_u_df <- read.table(header = T, "UPE_U_day.txt") %>% 
  subset(AirTemperature.C. != -999)

```

```{r, echo = FALSE}
##Create Target Table
#Create a table for only Year, Month, Day, Air temperature.
#Then, add a new binary column, 1 for >0C, 0 for <0C
UPE_L <- upe_l_df %>% 
  select(Year, MonthOfYear, AirTemperature.C.) 
  UPE_L$Melt <- as.numeric(UPE_L$AirTemperature.C. > 0)
  
UPE_U <- upe_u_df %>% 
  select(Year, MonthOfYear, DayOfMonth, AirTemperature.C.)
  UPE_U$Melt <- as.numeric(UPE_U$AirTemperature.C. > 0)
  
```
##Monthly and Annual Tables

###UPE L 
```{r, echo=FALSE}
upe_l_season <- UPE_L %>%
  rename(Month = MonthOfYear) %>% 
  mutate(Year = as.numeric(Year)) %>%
  group_by(Year, Month) %>%
  summarise(MeltDays = sum(Melt)) %>% 
  spread(Month, MeltDays)
upe_l_annual <- UPE_L %>%
  select(Year, Melt) %>% 
  mutate(Year = as.numeric(Year)) %>%
  group_by(Year) %>%
  summarise(MeltDays = sum(Melt))
  
datatable(
  upe_l_season, class = 'cell-border stripe', 
  rownames = FALSE, 
  colnames = c('Jan', 'Feb','Mar', 'Apr', 'May', 'Jun', 'July', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'),
  caption = htmltools::tags$caption(
  style = 'caption-side: bottom; text-align: center,',
  'Table 1: ', htmltools::em('This table displays the number of days per month with a temperature of > 0deg Celsius, 2009-2018')
  ))
datatable(
  upe_l_annual, class = 'cell-border stripe', 
  rownames = FALSE,
  caption = htmltools::tags$caption(
  style = 'caption-side: bottom; text-align: center,',
  'Table 1: ', htmltools::em('This table displays the number of days annually with a temperature of > 0deg Celsius, 2009-2018')
  ))
##c(Melt,Year(2009)))summarise(KAN_U, sum = sum(Melt))
#summarise(KAN_U, "2009" = sum(
```

### UPE U  

```{r, echo = FALSE}
upe_u_season <- UPE_U %>%
  rename(Month = MonthOfYear) %>% 
  mutate(Year = as.numeric(Year)) %>%
  group_by(Year, Month) %>%
  summarise(MeltDays = sum(Melt)) %>% 
  spread(Month, MeltDays)
upe_u_annual <- UPE_U %>%
  select(Year, Melt) %>% 
  mutate(Year = as.numeric(Year)) %>%
  group_by(Year) %>%
  summarise(MeltDays = sum(Melt))
  
datatable(
  upe_u_season, class = 'cell-border stripe', 
  rownames = FALSE, 
  colnames = c('Jan', 'Feb','Mar', 'Apr', 'May', 'Jun', 'July', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'),
  caption = htmltools::tags$caption(
  style = 'caption-side: bottom; text-align: center,',
  'Table 1: ', htmltools::em('This table displays the number of days per month with a temperature of > 0deg Celsius, 2009-2018')
  ))
datatable(
  upe_u_annual, class = 'cell-border stripe', 
  rownames = FALSE,
  caption = htmltools::tags$caption(
  style = 'caption-side: bottom; text-align: center,',
  'Table 1: ', htmltools::em('This table displays the number of days annually with a temperature of > 0deg Celsius, 2009-2018')
  ))
```

## Surface Temperature Timeseries

### UPE L   

```{r fig.height=10, fig.width=12, echo = FALSE}
upe_l_plot <- upe_l_df
#nuk_k_plot$Date <- as.Date(with(nuk_k_plot, paste(Year, MonthOfYear, DayOfMonth,sep="-")), "%d-%m-%Y")
   
   
ggplot(upe_l_plot, aes(DayOfYear, AirTemperature.C.))+
  geom_line()+
  facet_wrap(~Year,scales = "fixed", ncol = 2, nrow = 10)+
  xlab("Day of Year")+
  ylab("Degrees C")
  
  
  
  
```

### UPE U  

```{r fig.height=10, fig.width=12, echo = FALSE}
upe_u_plot <- upe_u_df
#kan_m_plot$Date <- as.Date(with(kan_m_plot, paste(Year, MonthOfYear, DayOfMonth,sep="-")), "%d-%m-%Y")
   
   
ggplot(upe_u_plot, aes(DayOfYear, AirTemperature.C.))+
  geom_line()+
  facet_wrap(~Year,scales = "fixed", ncol = 2)+
  xlab("Day of Year")+
  ylab("Degrees C")
  
  
```

#Thule (Qaanaaq) Transect

```{r, echo = FALSE}
thu_l_df <- read.table(header = T, "THU_L_day.txt") %>%  
  subset(AirTemperature.C. != -999)
thu_u_df <- read.table(header = T, "THU_U_day.txt") %>% 
  subset(AirTemperature.C. != -999)

```

```{r, echo = FALSE}
##Create Target Table
#Create a table for only Year, Month, Day, Air temperature.
#Then, add a new binary column, 1 for >0C, 0 for <0C
THU_L <- thu_l_df %>% 
  select(Year, MonthOfYear, AirTemperature.C.) 
  THU_L$Melt <- as.numeric(THU_L$AirTemperature.C. > 0)
  
THU_U <- thu_u_df %>% 
  select(Year, MonthOfYear, DayOfMonth, AirTemperature.C.)
  THU_U$Melt <- as.numeric(THU_U$AirTemperature.C. > 0)
  
```
##Monthly and Annual Tables

###THU L 
```{r, echo=FALSE}
thu_l_season <- THU_L %>%
  rename(Month = MonthOfYear) %>% 
  mutate(Year = as.numeric(Year)) %>%
  group_by(Year, Month) %>%
  summarise(MeltDays = sum(Melt)) %>% 
  spread(Month, MeltDays)
thu_l_annual <- THU_L %>%
  select(Year, Melt) %>% 
  mutate(Year = as.numeric(Year)) %>%
  group_by(Year) %>%
  summarise(MeltDays = sum(Melt))
  
datatable(
  thu_l_season, class = 'cell-border stripe', 
  rownames = FALSE, 
  colnames = c('Jan', 'Feb','Mar', 'Apr', 'May', 'Jun', 'July', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'),
  caption = htmltools::tags$caption(
  style = 'caption-side: bottom; text-align: center,',
  'Table 1: ', htmltools::em('This table displays the number of days per month with a temperature of > 0deg Celsius, 2009-2018')
  ))
datatable(
  thu_l_annual, class = 'cell-border stripe', 
  rownames = FALSE,
  caption = htmltools::tags$caption(
  style = 'caption-side: bottom; text-align: center,',
  'Table 1: ', htmltools::em('This table displays the number of days annually with a temperature of > 0deg Celsius, 2009-2018')
  ))
##c(Melt,Year(2009)))summarise(KAN_U, sum = sum(Melt))
#summarise(KAN_U, "2009" = sum(
```

### THU U  

```{r, echo = FALSE}
thu_u_season <- THU_U %>%
  rename(Month = MonthOfYear) %>% 
  mutate(Year = as.numeric(Year)) %>%
  group_by(Year, Month) %>%
  summarise(MeltDays = sum(Melt)) %>% 
  spread(Month, MeltDays)
thu_u_annual <- THU_U %>%
  select(Year, Melt) %>% 
  mutate(Year = as.numeric(Year)) %>%
  group_by(Year) %>%
  summarise(MeltDays = sum(Melt))
  
datatable(
  thu_u_season, class = 'cell-border stripe', 
  rownames = FALSE, 
  colnames = c('Jan', 'Feb','Mar', 'Apr', 'May', 'Jun', 'July', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'),
  caption = htmltools::tags$caption(
  style = 'caption-side: bottom; text-align: center,',
  'Table 1: ', htmltools::em('This table displays the number of days per month with a temperature of > 0deg Celsius, 2009-2018')
  ))
datatable(
  thu_u_annual, class = 'cell-border stripe', 
  rownames = FALSE,
  caption = htmltools::tags$caption(
  style = 'caption-side: bottom; text-align: center,',
  'Table 1: ', htmltools::em('This table displays the number of days annually with a temperature of > 0deg Celsius, 2009-2018')
  ))
```

## Surface Temperature Timeseries

### THU L   

```{r fig.height=10, fig.width=12, echo = FALSE}
thu_l_plot <- thu_l_df
#nuk_k_plot$Date <- as.Date(with(nuk_k_plot, paste(Year, MonthOfYear, DayOfMonth,sep="-")), "%d-%m-%Y")
   
   
ggplot(thu_l_plot, aes(DayOfYear, AirTemperature.C.))+
  geom_line()+
  facet_wrap(~Year,scales = "fixed", ncol = 2, nrow = 10)+
  xlab("Day of Year")+
  ylab("Degrees C")
  
  
  
  
```

### THU U  

```{r fig.height=10, fig.width=12, echo = FALSE}
thu_u_plot <- thu_u_df
#kan_m_plot$Date <- as.Date(with(kan_m_plot, paste(Year, MonthOfYear, DayOfMonth,sep="-")), "%d-%m-%Y")
   
   
ggplot(thu_u_plot, aes(DayOfYear, AirTemperature.C.))+
  geom_line()+
  facet_wrap(~Year,scales = "fixed", ncol = 2)+
  xlab("Day of Year")+
  ylab("Degrees C")
  
  
```

#Kronprins Christian Land Transect

```{r, echo = FALSE}
kpc_l_df <- read.table(header = T, "KPC_L_day.txt") %>%  
  subset(AirTemperature.C. != -999)
kpc_u_df <- read.table(header = T, "KPC_U_day.txt") %>% 
  subset(AirTemperature.C. != -999)

```

```{r, echo = FALSE}
##Create Target Table
#Create a table for only Year, Month, Day, Air temperature.
#Then, add a new binary column, 1 for >0C, 0 for <0C
KPC_L <- kpc_l_df %>% 
  select(Year, MonthOfYear, AirTemperature.C.) 
  KPC_L$Melt <- as.numeric(KPC_L$AirTemperature.C. > 0)
  
KPC_U <- kpc_u_df %>% 
  select(Year, MonthOfYear, DayOfMonth, AirTemperature.C.)
  KPC_U$Melt <- as.numeric(KPC_U$AirTemperature.C. > 0)
  
```
##Monthly and Annual Tables

###KPC L 
```{r, echo=FALSE}
kpc_l_season <- KPC_L %>%
  rename(Month = MonthOfYear) %>% 
  mutate(Year = as.numeric(Year)) %>%
  group_by(Year, Month) %>%
  summarise(MeltDays = sum(Melt)) %>% 
  spread(Month, MeltDays)
kpc_l_annual <- KPC_L %>%
  select(Year, Melt) %>% 
  mutate(Year = as.numeric(Year)) %>%
  group_by(Year) %>%
  summarise(MeltDays = sum(Melt))
  
datatable(
  kpc_l_season, class = 'cell-border stripe', 
  rownames = FALSE, 
  colnames = c('Jan', 'Feb','Mar', 'Apr', 'May', 'Jun', 'July', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'),
  caption = htmltools::tags$caption(
  style = 'caption-side: bottom; text-align: center,',
  'Table 1: ', htmltools::em('This table displays the number of days per month with a temperature of > 0deg Celsius, 2009-2018')
  ))
datatable(
  kpc_l_annual, class = 'cell-border stripe', 
  rownames = FALSE,
  caption = htmltools::tags$caption(
  style = 'caption-side: bottom; text-align: center,',
  'Table 1: ', htmltools::em('This table displays the number of days annually with a temperature of > 0deg Celsius, 2009-2018')
  ))
##c(Melt,Year(2009)))summarise(KAN_U, sum = sum(Melt))
#summarise(KAN_U, "2009" = sum(
```

### KPC U  

```{r, echo = FALSE}
kpc_u_season <- KPC_U %>%
  rename(Month = MonthOfYear) %>% 
  mutate(Year = as.numeric(Year)) %>%
  group_by(Year, Month) %>%
  summarise(MeltDays = sum(Melt)) %>% 
  spread(Month, MeltDays)
kpc_u_annual <- KPC_U %>%
  select(Year, Melt) %>% 
  mutate(Year = as.numeric(Year)) %>%
  group_by(Year) %>%
  summarise(MeltDays = sum(Melt))
  
datatable(
  kpc_u_season, class = 'cell-border stripe', 
  rownames = FALSE, 
  colnames = c('Jan', 'Feb','Mar', 'Apr', 'May', 'Jun', 'July', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'),
  caption = htmltools::tags$caption(
  style = 'caption-side: bottom; text-align: center,',
  'Table 1: ', htmltools::em('This table displays the number of days per month with a temperature of > 0deg Celsius, 2009-2018')
  ))
datatable(
  kpc_u_annual, class = 'cell-border stripe', 
  rownames = FALSE,
  caption = htmltools::tags$caption(
  style = 'caption-side: bottom; text-align: center,',
  'Table 1: ', htmltools::em('This table displays the number of days annually with a temperature of > 0deg Celsius, 2009-2018')
  ))
```

## Surface Temperature Timeseries

### KPC L   

```{r fig.height=10, fig.width=12, echo = FALSE}
kpc_l_plot <- kpc_l_df
#nuk_k_plot$Date <- as.Date(with(nuk_k_plot, paste(Year, MonthOfYear, DayOfMonth,sep="-")), "%d-%m-%Y")
   
   
ggplot(kpc_l_plot, aes(DayOfYear, AirTemperature.C.))+
  geom_line()+
  facet_wrap(~Year,scales = "fixed", ncol = 2, nrow = 10)+
  xlab("Day of Year")+
  ylab("Degrees C")
  
  
  
  
```

### KPC U  

```{r fig.height=10, fig.width=12, echo = FALSE}
kpc_u_plot <- kpc_u_df
#kan_m_plot$Date <- as.Date(with(kan_m_plot, paste(Year, MonthOfYear, DayOfMonth,sep="-")), "%d-%m-%Y")
   
   
ggplot(kpc_u_plot, aes(DayOfYear, AirTemperature.C.))+
  geom_line()+
  facet_wrap(~Year,scales = "fixed", ncol = 2)+
  xlab("Day of Year")+
  ylab("Degrees C")
```  
  
#SCO Transect

```{r, echo = FALSE}
sco_l_df <- read.table(header = T, "SCO_L_day.txt") %>%  
  subset(AirTemperature.C. != -999)
sco_u_df <- read.table(header = T, "SCO_U_day.txt") %>% 
  subset(AirTemperature.C. != -999)

```

```{r, echo = FALSE}
##Create Target Table
#Create a table for only Year, Month, Day, Air temperature.
#Then, add a new binary column, 1 for >0C, 0 for <0C
SCO_L <- sco_l_df %>% 
  select(Year, MonthOfYear, AirTemperature.C.) 
  SCO_L$Melt <- as.numeric(SCO_L$AirTemperature.C. > 0)
  
SCO_U <- sco_u_df %>% 
  select(Year, MonthOfYear, DayOfMonth, AirTemperature.C.)
  SCO_U$Melt <- as.numeric(SCO_U$AirTemperature.C. > 0)
  
```
##Monthly and Annual Tables

###SCO L 
```{r, echo=FALSE}
sco_l_season <- SCO_L %>%
  rename(Month = MonthOfYear) %>% 
  mutate(Year = as.numeric(Year)) %>%
  group_by(Year, Month) %>%
  summarise(MeltDays = sum(Melt)) %>% 
  spread(Month, MeltDays)
sco_l_annual <- SCO_L %>%
  select(Year, Melt) %>% 
  mutate(Year = as.numeric(Year)) %>%
  group_by(Year) %>%
  summarise(MeltDays = sum(Melt))
  
datatable(
  sco_l_season, class = 'cell-border stripe', 
  rownames = FALSE, 
  colnames = c('Jan', 'Feb','Mar', 'Apr', 'May', 'Jun', 'July', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'),
  caption = htmltools::tags$caption(
  style = 'caption-side: bottom; text-align: center,',
  'Table 1: ', htmltools::em('This table displays the number of days per month with a temperature of > 0deg Celsius, 2009-2018')
  ))
datatable(
  sco_l_annual, class = 'cell-border stripe', 
  rownames = FALSE,
  caption = htmltools::tags$caption(
  style = 'caption-side: bottom; text-align: center,',
  'Table 1: ', htmltools::em('This table displays the number of days annually with a temperature of > 0deg Celsius, 2009-2018')
  ))
##c(Melt,Year(2009)))summarise(KAN_U, sum = sum(Melt))
#summarise(KAN_U, "2009" = sum(
```

### SCO U  

```{r, echo = FALSE}
sco_u_season <- SCO_U %>%
  rename(Month = MonthOfYear) %>% 
  mutate(Year = as.numeric(Year)) %>%
  group_by(Year, Month) %>%
  summarise(MeltDays = sum(Melt)) %>% 
  spread(Month, MeltDays)
sco_u_annual <- SCO_U %>%
  select(Year, Melt) %>% 
  mutate(Year = as.numeric(Year)) %>%
  group_by(Year) %>%
  summarise(MeltDays = sum(Melt))
  
datatable(
  sco_u_season, class = 'cell-border stripe', 
  rownames = FALSE, 
  colnames = c('Jan', 'Feb','Mar', 'Apr', 'May', 'Jun', 'July', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'),
  caption = htmltools::tags$caption(
  style = 'caption-side: bottom; text-align: center,',
  'Table 1: ', htmltools::em('This table displays the number of days per month with a temperature of > 0deg Celsius, 2009-2018')
  ))
datatable(
  sco_u_annual, class = 'cell-border stripe', 
  rownames = FALSE,
  caption = htmltools::tags$caption(
  style = 'caption-side: bottom; text-align: center,',
  'Table 1: ', htmltools::em('This table displays the number of days annually with a temperature of > 0deg Celsius, 2009-2018')
  ))
```

## Surface Temperature Timeseries

### SCO L   

```{r fig.height=10, fig.width=12, echo = FALSE}
sco_l_plot <- sco_l_df
#nuk_k_plot$Date <- as.Date(with(nuk_k_plot, paste(Year, MonthOfYear, DayOfMonth,sep="-")), "%d-%m-%Y")
   
   
ggplot(sco_l_plot, aes(DayOfYear, AirTemperature.C.))+
  geom_line()+
  facet_wrap(~Year,scales = "fixed", ncol = 2, nrow = 10)+
  xlab("Day of Year")+
  ylab("Degrees C")
  
  
  
  
```

### SCO U  

```{r fig.height=10, fig.width=12, echo = FALSE}
sco_u_plot <- sco_u_df
#kan_m_plot$Date <- as.Date(with(kan_m_plot, paste(Year, MonthOfYear, DayOfMonth,sep="-")), "%d-%m-%Y")
   
   
ggplot(sco_u_plot, aes(DayOfYear, AirTemperature.C.))+
  geom_line()+
  facet_wrap(~Year,scales = "fixed", ncol = 2)+
  xlab("Day of Year")+
  ylab("Degrees C")
```  

#EGP

```{r, echo = FALSE}
egp_df <- read.table(header = T, "EGP_day.txt") %>%  
  subset(AirTemperature.C. != -999)

```

```{r, echo = FALSE}
##Create Target Table
#Create a table for only Year, Month, Day, Air temperature.
#Then, add a new binary column, 1 for >0C, 0 for <0C
EGP <- egp_df %>% 
  select(Year, MonthOfYear, AirTemperature.C.) 
  EGP$Melt <- as.numeric(EGP$AirTemperature.C. > 0)
  
  
```
##Monthly and Annual Tables

###EGP 
```{r, echo=FALSE}
egp_season <- EGP %>%
  rename(Month = MonthOfYear) %>% 
  mutate(Year = as.numeric(Year)) %>%
  group_by(Year, Month) %>%
  summarise(MeltDays = sum(Melt)) %>% 
  spread(Month, MeltDays)
egp_annual <- EGP %>%
  select(Year, Melt) %>% 
  mutate(Year = as.numeric(Year)) %>%
  group_by(Year) %>%
  summarise(MeltDays = sum(Melt))
  
datatable(
  egp_season, class = 'cell-border stripe', 
  rownames = FALSE, 
  colnames = c('Jan', 'Feb','Mar', 'Apr', 'May', 'Jun', 'July', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'),
  caption = htmltools::tags$caption(
  style = 'caption-side: bottom; text-align: center,',
  'Table 1: ', htmltools::em('This table displays the number of days per month with a temperature of > 0deg Celsius, 2009-2018')
  ))
datatable(
  egp_annual, class = 'cell-border stripe', 
  rownames = FALSE,
  caption = htmltools::tags$caption(
  style = 'caption-side: bottom; text-align: center,',
  'Table 1: ', htmltools::em('This table displays the number of days annually with a temperature of > 0deg Celsius, 2009-2018')
  ))
##c(Melt,Year(2009)))summarise(KAN_U, sum = sum(Melt))
#summarise(KAN_U, "2009" = sum(
```


## Surface Temperature Timeseries

### EGP   

```{r fig.height=10, fig.width=12, echo = FALSE}
egp_plot <- egp_df
#nuk_k_plot$Date <- as.Date(with(nuk_k_plot, paste(Year, MonthOfYear, DayOfMonth,sep="-")), "%d-%m-%Y")
   
   
ggplot(egp_plot, aes(DayOfYear, AirTemperature.C.))+
  geom_line()+
  facet_wrap(~Year,scales = "fixed", ncol = 2, nrow = 10)+
  xlab("Day of Year")+
  ylab("Degrees C")
  
  
  
  
```
